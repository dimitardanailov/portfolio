import {InternalMetadata} from '@/seo/metadata'
import {experienceByYears} from '@/constants/text'

const title = `${experienceByYears} years of helping build an inclusive web2 or web3 projects`
const description =
  'Full Stack Engineer with 15 Years Experience. Seeking Staff Engineer, Team Lead, or Architect Roles. Top 4% on Stack Overflow, Leadership roles: 12+ years'
const canonical = ''

const props: InternalMetadata = {
  title,
  description,
  canonical,
}

export default props

export {title}
