import {InternalMetadata} from '@/seo/metadata'

const title = 'React, REST API, React Custom Hooks'
const description = 'Type of demos: widgets, tables, coingecko fetchers'
const canonical = '/demos'

const props: InternalMetadata = {
  title,
  description,
  canonical,
}

export default props

export {title, description}
