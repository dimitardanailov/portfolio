import {InternalMetadata} from '@/seo/metadata'

const title = 'Certified by industry giants: Google, Meta, AWS'
const description =
  'Certification in the following areas: Blockchain, AWS, Docker, Kubernetes, Smart Contracts, React'
const canonical = '/certificates'

const props: InternalMetadata = {
  title,
  description,
  canonical,
}

export default props

export {title}
