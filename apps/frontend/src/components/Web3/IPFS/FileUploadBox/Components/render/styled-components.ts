import styled from 'styled-components'

const Wrapper = styled.div`
  position: relative;

  display: block;
  margin: 1rem 0 2rem;
`

const WrapperButttons = styled.div`
  position: relative;

  display: flex;
  flex-direction: row;
`

export {Wrapper, WrapperButttons}
