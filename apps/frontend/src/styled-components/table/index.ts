import styled from 'styled-components'

const StyledTableHeaderCell = styled.span`
  position: relative;

  font-weight: bold;
`

const StyledTableRowCell = styled.div`
  position: relative;

  display: flex;
`

export {StyledTableHeaderCell, StyledTableRowCell}
